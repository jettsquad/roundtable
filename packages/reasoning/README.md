# @squad/reasoning — Lil X（`ctx.reasoning`）

使用者自己的**判断标准库**。四个插件里的 ④。

## 拥有

- 判据（抽象）与实例（原始情境）的存放格式与目录
- 处境三轴：框架位置 / 动作类型（闭合 7）/ 特征标记（闭合 8）
- 捕获：把「人推翻了机器」这件事记成实例
- 提炼：起草一条**待裁定**的提议（关系判定 + 抽象表述）
- 裁定：人 accept / reject / 改写后接受
- 定位：按**处境**粗筛，再交模型精选（`deliver.ts`）
- 投放文本：系统通道的呈现形式，含硬预算
- 校验：三个刻度、体检、主动挂起复审（`usage.ts`）

## 绝不拥有

| 不拥有 | 归谁 |
|---|---|
| 任何团队状态 | ① `ctx.teams` —— **`inject` 里刻意没有 `teams`** |
| 检查点、议程的文本判断 | ③ `ctx.secretary` |
| 「席位这轮看见什么」 | ② `ctx.teamContext` |

**它是用户级的，不是团队级的。**一个只能从项目内部写入的库，正好是「可以带走的资产」的反面。

## 三条不可动摇的

**只捕获程序零误判的信号。**当前只有一种：人否决了机器的提议。**明确不做**从对话里自动挖掘——
噪声大，而且会大量捕到**结论**（「他选了 Postgres」）而不是**标准**（「他要求给出迁移成本估算
才接受选型」）。这个区分是整件事的生死线。

**人裁定不能省。**这里没有任何东西会自己生效。系统在修改自己的评分标准，自动通过等于让它
自己给自己打分。一切落在 `proposals/`，等人。

**实例先写，再去问模型。**发生过的事是事实，提炼是对它的看法。模型调用失败时事实必须已经在盘上——
反过来写，丢掉的正好是系统最没把握的那些случае，而那些才是值钱的。

## 反例为什么是四种关系里最重要的一种

反例是系统能收到的**最大梯度**。它意味着两件事之一：判据错了（推翻），或判据的适用边界比
原来写的窄（收窄）。**第二种更常见，也更有价值。**

「不可见 = 不可纠正」这条的边界——*动作平凡可逆时不适用（没人要求每次自动格式化都弹窗）*——
**只可能从反例里学到**。没有反例通道，抽象会随着每次确认变得更绝对，最后变成一句没有适用
条件的口号。

## 存放：结构负责被找到，散文负责被读懂

```
<root>/
  criteria/    抽象 —— 可迁移、可以给别人
  instances/   实例 —— 本人的、带项目细节、不出门
  proposals/   待人裁定
```

两个极端都不行：纯散文没法判断「要不要捞它」（每次都要模型读全部）；纯结构化把「为什么」
压扁了，而「为什么」是判据里唯一值钱的部分。所以 front matter 放触发条件，正文放主张与边界。

**这个模式已在小尺度上验证过**：检查点的四个固定标题是结构，正文是判断。同一思路放大。

## 导出边界就是目录切分

> **抽象可导出，实例不可。**

这不需要额外设计，也不需要任何隐私标记：它是「文件在哪个目录」这个事实，不是某个未来的导出
程序需要记住的规则。同一个决定同时解决了「出厂初级版」和「隐私」两件事。

## 为什么现在没有相似度计算

主键是**处境**，不是文本。「不可见 = 不可纠正」学自检查点折叠，**该触发于自动重试、自动降级**
——文字上八竿子打不着，处境上完全同构。按文字找永远找不回来。

而这恰恰是最值钱的一次召回：同话题的召回人自己也想得起来，**跨话题的只有系统能提醒**。

以单用户规模，触发条件粗过滤之后把候选全给模型读，比任何相似度算法都准——因为**模型读得懂
「适用边界」，算法读不懂**。先做对，再做快；过早上这层会把「触发条件怎么设计」这个真正的
难题掩盖掉。

## 投放：给谁，是拓扑决定的

> **只投给主持人侧和秘书，绝不投给讨论席位。**

判据影响「怎么组织、怎么评判」，不影响「怎么想」。参与者要保持天然多样——这是回音室
（R1）的主要防御。

**而它必须是拓扑事实，不是纪律。**实现方式：`locate()` / `brief()` 把结果**返回给调用方**，
本插件不持有任何指向桌子的引用，也不注入 `teams`。于是**不存在**一条从判据库通往席位 prompt
的边——不是"没人这么写"，是写不出来。

冒烟对此有直接断言：召回一条判据之后，检查席位实际会拿到的窗口里**没有它的主张**。

## 硬预算：最多 3 条

预算在**产出列表的地方**执行，不留给渲染方。理由：投多了整块会被跳过，那比一条都不投更糟——
既花掉了预算，又花掉了注意力。

提示词里也写了「宁少勿多」，但**依赖模型守规矩的预算不是预算**，所以解析之后还会再截一次。

## 投放时必须带的三样

| 带上什么 | 为什么 |
|---|---|
| 标明**是判据不是指令** | 与检查点注入同一道围栏，同一个理由 |
| 标明**从哪几条实例长出来** | 错误的抽象要能一眼看穿，而不是让人信系统的话 |
| 标明**它现在生效** | 目的就是被纠正，而没人会去纠正一件他不知道在运行的事 |

## 精选：什么时候不叫模型

粗筛之后如果本来就不超过 3 条，**不叫模型**。花一次调用去从 3 条里挑 3 条买不到任何东西，
而一个不可能改变答案的调用只可能失败。

## 粗过滤为什么是「重叠」而不是「包含」

判据读起来是合取（`特征 ⊇ {自动触发, 结果不可见}`），我最初也是这么实现的。然后端到端撞上了：

- 提炼把判据存成 `{automatic, invisible-result}`
- 秘书把「设计自动折叠机制」这个阶段标成 `{automatic, changes-default}`
- 严格包含 → **不匹配**。一条从「设计自动折叠机制」学来的判据，在「设计自动折叠机制」的阶段上不触发

两个标签都站得住。**两次独立的模型调用从闭合列表里取标签、没有共同锚点，这种分歧是结构性的**，
调哪一边的提示词都治不好。

定案靠的是代价不对称：

| | 可恢复吗 |
|---|---|
| 捞多了 | **可以**——精选那一步会读主张和边界把它丢掉，后面还有 3 条硬上限 |
| 捞少了 | **不行**——它根本不会出现在任何能判断它的东西面前 |

而「从没被捞出来过」正是设计文档列的第一个失败模式。**一个要求标签精确一致的粗过滤，不叫粗过滤。**

配套的一处改动：精选**只要有候选就跑**，不再是「超过 3 条才跑」。过滤严格时那条理由成立
（一个不可能改变答案的调用只可能失败），现在过滤故意放宽了，**放宽之所以安全，正是因为下游
有人在读适用边界**。

## 三个刻度，以及第三个为什么最危险

| 刻度 | 测什么 | 不合格的样子 |
|---|---|---|
| 被投放次数 | 相关性 | 从没被捞出来过 → **触发条件写错了**，不是这条主张不受欢迎 |
| 投放后是否改变结果 | 有效性 | 长期投放却从不改变任何决定 → 存疑 |
| 有没有出现过反例 | 真实性 | **攒了很多证据却从未出现反例 → 可疑** |

第三条最危险，因为**它的警报长得像好消息**。一条谁都反驳不了的判据，和一条笼统到无法被反驳的
判据，从外面看一模一样——所以证据攒到 5 条仍无反例时，会被**主动挂起**为 `suspect`。

`suspect` 仍然照常投放：它是「复审中」不是「已撤销」，藏起来恰好会挡掉最可能带来反例、
从而了结它的那次机会。

## 有一个刻度程序量不了，所以它不猜

「投放后是否改变结果」需要知道一个决定**是否因为它而变**——从这里看不见。所以它是**人留下的标记**
（`markOutcome`），不是推断出来的数。

给一个量不出来的东西编个代理指标，会产出一个**看起来像测量的数字**，那比留空更糟：留空会招人来填。

配套的一条：**没有任何标记 ≠ 标记为无用**。20 次投放零标记判为 `ok`，不判为 inert——沉默不是证据。

## 计数在「投放」而不是「候选」

「从没被捞出来过」必须意味着**从没被放到任何人面前**。按候选计数的话，第一个刻度量的是过滤器的
行为，而过滤器正是这个刻度本该指控的东西。

## 使用数据在导出边界的本地一侧

`usage/` 和 `instances/` 同侧。拿到别人的抽象不该拿到他的投放次数；把自己的送出去，也不该
连带交出自己是怎么工作的。实测：判据文件里一个计数字段都没有。

写入按判据串行。并发的读-改-写会丢增量，而**少计是看不见的**——它和「这条判据被捞得少」长得一样，
而那正是第一个刻度要给出的信号。
