# @squad/shared

Squad 1.x 验证过的纯逻辑，原样搬过来，连测试一起。

## 拥有

- 检查点的结构、提示词与校验（`team-checkpoint`）
- 折叠阈值：距上次检查点的累计量、CJK/拉丁分开估算（`team-checkpoint-threshold`）
- 产出路径的规范化与多席位分文件（`team-artifact`）
- 从回复里剥掉模型的思考过程（`agent-reply-text`）
- 议程中止交接文档的结构与校验（`team-agenda-termination`）

## 绝不拥有

**任何行为。**这里只有纯函数：给定输入，返回输出。

- 不 import `@deepseek-ai/*`——碰不到 `ctx`，也就碰不到任何服务
- 不 import `@squad/*`——它是最底层
- 不做 I/O

这三条**由 eslint 强制**（`no-restricted-imports`），不是约定。

理由：这些模块分属不同插件的领域（检查点归秘书与装配器，产出路径归桌子）。
集中放是为了让测试跟着一起搬、让多个插件复用同一份判断。
一旦它能拿到服务，它就变成了行为的第二个家，插件边界就只剩纸上的了。

## 为什么它们值得单独一包

这是 1.x 唯一可以原样继承的资产。它们决定「一个 agent 看见什么」「讨论什么时候被折叠」
「产出写到哪」「什么算回答」——都是 1.x 最难做对的部分，每一条都有踩坑记录写在注释里。

**测试是搬迁的验收标准**：51 个测试跟着源码一起过来，全绿才算搬对了。

## 只出不进

技术设计 §2 把这里定义为「**从 1.x 搬运的落点**」——落点是个一次性的词，这一节把它写成规矩。

**阶段 2 起，本包不新增文件。**

- 新写的纯函数默认放进**消费它的那个插件包**里
- 只有当**第二个**插件真的要用时，才提升到这里
- 提升要在 PR 里点名两个消费者，「看起来挺通用」不是理由

三条 import 禁令挡的是「长行为」；这一条挡的是「长文件」。杂物抽屉的实际形成方式
不是某天有人塞进来一个 `ctx`，而是每周多一个看着挺通用的纯函数——那种增长完全合规，
所以规则拦不住，只能靠这条约定。

这条规矩自带退出条件：它预期本包**变小**，不是变大。

## 待迁出

| 目前在这 | 该去哪 | 什么时候 |
|---|---|---|
| `buildCheckpointPrompt` · `validateCheckpoint` · `CHECKPOINT_HEADINGS` | ③ `@squad/secretary` | `packages/secretary` 建立时，跟着走 |

理由：它们形式上是纯函数（输入 → 字符串），三条禁令一条不犯。但 `buildCheckpointPrompt`
的**内容是一个决定——秘书被告知要做什么**。那不是共享的判断逻辑，那是秘书插件的核心资产，
只是碰巧用不需要 `ctx` 的方式写出来了。

留到现在是因为阶段 1 没有秘书插件，搬无可搬。列在这里是为了它别靠「没人再看一眼」留下来。
